/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["links.icart.tn","linktree.sirv.com", "firebasestorage.googleapis.com"],
    }
}

module.exports = nextConfig