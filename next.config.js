/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["link.icart.tn","linktree.sirv.com", "firebasestorage.googleapis.com"],
    }
}

module.exports = nextConfig